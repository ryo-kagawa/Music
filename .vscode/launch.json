{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch Package",
      "type": "go",
      "request": "launch",
      "mode": "auto",
      "program": "${fileDirname}",
      "args": [
        "${input:input1}"
      ],
      "cwd": "${workspaceFolder}",
      "windows": {
        "output": "${workspaceFolder}\\__debug_bin.exe"
      }
    }
  ],
  "inputs": [
    {
      "id": "input1",
      "type": "promptString",
      "description": "引数を入力"
    }
  ]
}